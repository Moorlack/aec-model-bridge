# Structural workflows

Confirm levels and inspect existing grids, supports, framing, and loaded types. Create datum first, then foundations and columns, then beams; verify the first bay before repeating. Check member endpoints, levels, offsets, and types. Use warnings and clash checks for model QA. Do not invent a structural section, grade, or specialist analytical setting; use an approved raw API operation only if no typed tool exists.
