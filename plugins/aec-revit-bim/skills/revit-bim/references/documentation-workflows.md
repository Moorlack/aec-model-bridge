# Documentation workflows

Inspect views and templates, create required plan/section/3D views, then apply the intended template. Annotate verified geometry with dimensions, tags, and notes. Create and inspect schedules before assembling sheets and placing valid viewports. Export only after final review and only to an approved path.
